
&НаКлиенте
Процедура ИзменитьСостав(Команда)
	Если ЭтаФорма.Модифицированность тогда
		Ответ=Вопрос("Необходимо прежде сохранить элемент. Продолжить?",РежимДиалогаВопрос.ОКОтмена);
		Если Ответ=КодВозвратаДиалога.Отмена тогда
			Возврат;
		Иначе
			Записать();
		КонецЕсли;
	КонецЕсли;
	ОткрытьФормуМодально("Обработка.УстановитьСоставГруппыДО.Форма.Форма",Новый Структура("ГруппаДО",Объект.Ссылка));
КонецПроцедуры
